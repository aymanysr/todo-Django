// document.addEventListener('DOMContentLoaded', function () {
//   document.querySelectorAll('.mark-todo').forEach(function (button) {
//       button.addEventListener('click', function () {
//           // Get the todo item ID from the button's data attribute
//           var todoId = this.dataset.todoId;

//           // Check if the element with the specified ID exists
//           var todoItem = document.getElementById('todo-' + todoId);

//           if (todoItem) {
//               // Toggle the 'marked' class on the corresponding todo item
//               todoItem.classList.toggle('marked');
//           } else {
//               console.error('Element not found:', 'todo-' + todoId);
//           }
//       });
//   });
// });
